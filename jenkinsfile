pipeline {
  agent any

  environment {
    MAJOR_VERSION = 1
  }

  stages {
    stage('Build') {
	steps {
      		sh 'ant -f build.xml -v'
		echo "hello world"
	      }
	
	post{
	   always{
		
		archiveArtifacts artifacts: 'dist/.*jar',fingerprints:true
		}
			}
   		 }
}
}   

