pipeline {
  agent any

  stages {
    stage('Build') {
	steps {
      		sh 'ant -f build.xml -v'
		echo "hello world"
	      }
	}
    }
	post {
	   always {
		archiveArtifacts artifacts: 'dist/.*jar', fingerprint: true
		}
	    }
  
}

